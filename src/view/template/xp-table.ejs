<p>
    You currently have <span class="xp"><%= formatNumber(currentXp) %> XP</span>, 
    <% if (currentLevel < maxLevel) { %>
        but you need <span class="xp"><%= formatNumber(xpNeeded) %> XP</span> more to reach Level <%= currentLevel + 1 %>.
    <% } else { %>
        and you're already max level!
    <% } %>
</p>

<div class="xp-table-grid">
    <% columns.forEach(function(col, ci) { %>
        <table class="data-table">
            <thead>
                <tr>
                    <th class="center">Level</th>
                    <th>XP</th>
                </tr>
            </thead>
            <tbody>
                <% col.forEach(function(row) { %>
                    <tr<% if (row.rowClass === 'row-current') { %> class="row-current"<% } %>>
                        <td class="center">
                            <%= row.level %>
                        </td>
                        <td class="xp">
                            <%= formatNumber(row.xpReq) %>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    <% }); %>
</div>

<p class="last">
    <a href="/">Go back to the journey</a>
</p>
