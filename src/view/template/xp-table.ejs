<p>
    You currently have <span class="xp"><%= currentXpFormatted %> XP</span>, 
    <% if (currentLevel < maxLevel) { %>
        but you need <span class="xp"><%= xpNeededFormatted %> XP</span> more to reach Level <%= nextLevelFormatted %>.
    <% } else { %>
        and you're already max level!
    <% } %>
</p>

<div class="xp-table-grid">
    <% columns.forEach(function(col, ci) { %>
        <table class="data-table">
            <thead>
                <tr>
                    <th class="center">Level</th>
                    <th>XP</th>
                </tr>
            </thead>
            <tbody>
                <% col.forEach(function(row) { %>
                    <tr<% if (row.rowClass === 'row-current') { %> class="row-current"<% } %>>
                        <td class="center">
                            <%= row.levelFormatted %>
                        </td>
                        <td class="xp">
                            <%= row.xpReqFormatted %>
                        </td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    <% }); %>
</div>

<p class="last">
    <a href="/">Go back to the journey</a>
</p>
