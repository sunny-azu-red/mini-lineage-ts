<% if (alertHtml) { %>
    <div class="alert alert-success"><%- alertHtml %></div>
<% } %>

<p>
    You have arrived at the Inn.<br>
    The young lady greets you and sets you at a table.
</p>

<table class="data-table">
    <thead>
        <tr>
            <th>Name</th>
            <th>Healing</th>
            <th>Adena</th>
        </tr>
    </thead>
    <tbody>
        <% foods.forEach(function(food, i) { %>
            <tr>
                <td><%= food.name %></td>
                <td><%= food.stat %></td>
                <td class="gold"><%= food.costFormatted %></td>
            </tr>
        <% }); %>
    </tbody>
</table>

<form method="POST" action="/inn">
    <div class="form-row">
        <select class="form-select" name="select_food" id="inn-select">
            <option value="">🚪 Home Town</option>
            <% selectFoods.forEach(function(f) { %>
                <option value="<%= f.id %>">Pick <%= f.emoji %> <%= f.name %></option>
            <% }); %>
        </select>
        <button type="submit" class="btn" id="inn-btn">Travel</button>
    </div>
</form>
<script>
    (function () {
        var sel = document.getElementById('inn-select');
        var btn = document.getElementById('inn-btn');
        sel.addEventListener('change', function () {
            btn.textContent = sel.value === '' ? 'Travel' : '🪙 Order';
        });
    })();
</script>