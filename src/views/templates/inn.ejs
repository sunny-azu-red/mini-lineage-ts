<% if (alertHtml) { %>
    <div class="alert alert-info"><%- alertHtml %></div>
    <% } %>

        You have arrived at the Inn.<br>
        The old lady greets you and sets you at a table.<br>
        You see a menu and you open it. Inside you read:<br><br>

        <table class="data-table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Healing</th>
                    <th>Adena</th>
                </tr>
            </thead>
            <tbody>
                <% foods.forEach(function(food, i) { %>
                    <tr class="<%= i % 2 === 0 ? 'row-even' : 'row-odd' %>">
                        <td>
                            <%= food.name %>
                        </td>
                        <td>+<%= food.stat %> HP</td>
                        <td class="gold">
                            <%= food.costFormatted %>
                        </td>
                    </tr>
                    <% }); %>
            </tbody>
        </table>
        <br>

        <form method="POST" action="/inn">
            <div class="form-row">
                <select class="form-select" name="select_food">
                    <option value="">🚪 Leave Inn</option>
                    <% foods.forEach(function(food, i) { %>
                        <option value="<%= i %>">Buy <%= food.name %>
                        </option>
                        <% }); %>
                </select>
                <button type="submit" class="btn">Submit</button>
            </div>
        </form>